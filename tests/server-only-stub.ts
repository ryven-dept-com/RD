// Test-time stub for Next.js's "server-only" guard. Unit tests exercise the
// pure logic of server modules in a plain Node environment; the real guard
// still applies in the Next.js build.
export {};
